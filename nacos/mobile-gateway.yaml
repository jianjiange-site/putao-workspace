# Nacos Data ID: mobile-gateway.yaml
# Namespace: putao-dating-dev

spring:
  # PostgreSQL
  datasource:
    url: jdbc:postgresql://38.76.188.242:5433/putao_dating_dev?currentSchema=gateway&sslmode=disable
    username: jianjian_test
    password: MpR5rGjss2Ly6vJFAhaxAwNqVAGVoP7V

  # Redis
  data:
    redis:
      host: 38.76.188.242
      port: 6380
      password: sNuP9gZScsj88QbEyTujffOvRCCH9Kv1
      database: 1
      timeout: 5000ms
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 2
          max-wait: 5000ms

  # Flyway
  flyway:
    baseline-on-migrate: true
    baseline-version: 0
    placeholder-replacement: false

# gRPC Client hosts (通过环境变量覆盖)
user:
  service:
    grpc:
      host: ${USER_SERVICE_HOST:localhost}
      port: ${USER_SERVICE_PORT:19090}

im:
  service:
    grpc:
      host: ${IM_SERVICE_HOST:localhost}
      port: ${IM_SERVICE_PORT:19091}

post:
  service:
    grpc:
      host: ${POST_SERVICE_HOST:localhost}
      port: ${POST_SERVICE_PORT:19084}

match:
  service:
    grpc:
      host: ${MATCH_SERVICE_HOST:localhost}
      port: ${MATCH_SERVICE_PORT:19092}

payment:
  service:
    grpc:
      host: ${PAYMENT_SERVICE_HOST:localhost}
      port: ${PAYMENT_SERVICE_PORT:19093}

# JWT Configuration
jwt:
  private-key-base64: ${JWT_PRIVATE_KEY:}
  public-key-base64: ${JWT_PUBLIC_KEY:}
  access-token-expiry-seconds: 900
  refresh-token-expiry-days: 7
  issuer: dating-app

# Upload Configuration
app:
  upload:
    endpoint: https://minio-api.jianjiange.site
    access-key: admin
    secret-key: GorLDkuOhGyK5c1RXh2gaPooXgtso/MR
    bucket: putao-dating-dev
    path-style-access: true

# Logging
logging:
  level:
    root: INFO
    com.dating.gateway: DEBUG
