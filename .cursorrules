# Dating Project Cursor 开发规则

> 基于 student-dev-guide.md 自动生成，vibe-coding 时请严格遵守

---

## 项目概述

- **类型**：约会交友 App 后端（微服务架构）
- **技术栈**：Java 21 / Spring Boot 3.3.5 / PostgreSQL / Redis / gRPC / Nacos
- **仓库结构**：monorepo，含 `ai-chat`/`dating-server`/`proto` 三个顶层目录

---

## 技术栈约束

- **DB**：只用 PostgreSQL（TIMESTAMPTZ 字段），禁止 MySQL
- **缓存**：Redis 7，key 必须带前缀 `putao:<service>:<domain>:<id>`
- **服务间调用**：只能用 gRPC，禁止 HTTP/Feign/RestTemplate
- **ORM**：MyBatis-Plus，单表操作，一张 Mapper 只查一张表，禁止 JOIN
- **配置**：敏感信息放 Nacos，绝不硬编码密码/token/私钥

---

## 目录分层约定

```
com.dating.<service>/
├── controller/    # HTTP 入口，只做参数校验 + 调 service
├── grpc/         # gRPC 服务端实现
├── service/      # 业务编排，事务边界
├── manager/      # 数据访问编排，包装多次 mapper + 缓存
├── mapper/       # MyBatis-Plus Mapper，一张表一个
├── entity/       # 数据库实体
├── dto/          # 入参
├── vo/           # 出参
├── client/       # 调用其他服务的 gRPC stub
└── exception/    # 业务异常
```

**调用方向**：controller/grpc → service → manager → mapper（单向，禁止逆流）

---

## 代码规范

### 命名
- 类：UpperCamel（例：`UserProfileService`）
- 方法/变量：lowerCamel（例：`getUserProfile`）
- 常量：UPPER_SNAKE（例：`MAX_RETRY_COUNT`）
- DTO 后缀：`Req`/`Resp`/`DTO`，VO 后缀：`VO`

### 注释要求（强制）
1. **每个方法必须有 Javadoc**：功能描述 + @param + @return + @throws
2. **函数内每个逻辑块用单行注释划清**（带编号更好）

```java
/**
 * 根据 user_id 查询用户档案，命中缓存返回，未命中回源 DB 并回填。
 *
 * @param userId 业务主键，必填 > 0
 * @return 用户档案 VO；不存在时抛 UserNotFoundException
 * @throws UserNotFoundException 用户不存在或已删除
 */
public UserProfileVO getProfile(Long userId) { ... }
```

### 异常处理
- 业务异常继承 `BizException(code, message)`
- 全局 `@RestControllerAdvice` 兜底转 `Result.fail(code, message)`
- 禁止在 controller 抛 `throw new Exception()`

### 事务
- `@Transactional(rollbackFor = Exception.class)` 加在 service 方法上
- 禁止跨服务分布式事务

---

## 时区规则

**全系统统一 UTC**：
- DB 列用 `TIMESTAMPTZ`，连接 `SET TIME ZONE 'UTC'`
- JVM `-Duser.timezone=UTC`
- 代码/SQL 禁止写死 `Asia/Shanghai`
- 本地时区只在 App 展示层转换

---

## Redis 规范

- **Key 格式**：`putao:<service>:<domain>:<id>`
  - ✅ `putao:user:profile:1024`
  - ❌ `user:profile:1024`（没前缀会撞车）
- **TTL 必须设置**：禁止永久 key
- **缓存策略**：cache aside（先写库，再删缓存）
- **禁止 FLUSHDB/FLUSHALL**

---

## 对象存储（MinIO）

- **Endpoint**：`https://minio-api.jianjiange.site`
- **必须配置**：`path-style-access: true`
- **Object Key 格式**：`<category>/<owner_id>/<yyyymm>/<uuid>.<ext>`
- **出参**：VO 只回 `*_key`，让 App 自拼 URL

---

## gRPC 规范

- `.proto` 文件在 workspace 根的 `proto/` 目录
- 包坐标必须带putao前缀：`com.dating.<name>.proto:*`
- 改了 proto 必须：1) 升版本号 2) 重新 deploy 到 Nexus 3) 依赖方同步升级

---

## Git 规范

### 分支
- `main`：受保护，只接 dev 合入
- `dev`：集成主线
- `<your>/<topic>`：个人特性分支

### Commit Message（Conventional Commits）
```
<type>(<service>): <概要>
feat(im-service): 支持 LiveKit Token 短 TTL 重签
fix(payment-service): 修复金币消费幂等 key 与并发的竞争
```

### 类型
- `feat`：新功能
- `fix`：bug 修复
- `refactor`：重构
- `docs`：文档
- `chore`：构建/CI/依赖

---

## 红线（违反一票否决）

1. ❌ 持久层出现多表 JOIN
2. ❌ 跨服务直连别人家的库表/Redis/对象桶
3. ❌ 服务间用 HTTP/Feign/RestTemplate 代替 gRPC
4. ❌ 真实密码/token/私钥进 git
5. ❌ DB 列用 `TIMESTAMP`（无时区）或代码写死 `Asia/Shanghai`
6. ❌ 业务服务自建 WebSocket 或绕开 im-service 调 OpenIM
7. ❌ 跨服务分布式事务
8. ❌ controller 直接调 mapper
9. ❌ 对外 API 暴露内部自增 `id`（应用业务主键 `<entity>_id`）
10. ❌ 把 Redis 当数据库

---

## 本地开发

- 构建命令：`mvn -B -ntp clean package`
- 启动配置：`SPRING_PROFILES_ACTIVE=dev`
- 敏感配置：从环境变量读，不硬编码

---

## Spring Boot 配置 / Nacos 接入规范（强制）

> 这一章是启动期踩坑最多的地方。新建任何服务前先把本节读完。

### 1. 配置分层原则

- **application.yml 写死**：端口、应用名、`spring.config.import`、nacos 客户端连接信息（server-addr/namespace/账号）、grpc 端口、snowflake id、key 前缀、MyBatis-Plus 配置、logging 格式
- **Nacos DataID 写**：敏感信息（数据库 URL/账号/密码、Redis 密码、JWT key、RocketMQ 凭据、对象存储 secret）、外部服务地址
- **绝不在 application.yml 写数据库密码或第三方密钥**（红线 #4）

### 2. Nacos 接入样板（application.yml 必备项，复制即可）

```yaml
server:
  port: ${<SERVICE>_PORT:18XXX}

spring:
  application:
    name: dating-<service>-service        # 必须带 dating- 前缀
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  config:
    import: "optional:nacos:dating-<service>-service-dev.yaml"   # 注意带 -dev 后缀
  datasource:
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 8
      minimum-idle: 2
      connection-init-sql: SET TIME ZONE 'UTC'                    # 强制 UTC
  data:
    redis:
      timeout: 3s
  cloud:
    nacos:
      discovery:                                                  # ⚠️ 两段都要写
        enabled: true
        server-addr: ${NACOS_SERVER_ADDR:38.76.188.242:8848}
        namespace: ${NACOS_NAMESPACE:putao-dating-dev}
        username: ${NACOS_USERNAME:nacos}
        password: ${NACOS_PASSWORD:jianjiange}
      config:                                                     # ⚠️ 不能漏
        enabled: true
        import-check:
          enabled: true
        server-addr: ${NACOS_SERVER_ADDR:38.76.188.242:8848}
        namespace: ${NACOS_NAMESPACE:putao-dating-dev}
        username: ${NACOS_USERNAME:nacos}
        password: ${NACOS_PASSWORD:jianjiange}
        file-extension: yaml
        group: DEFAULT_GROUP

app:
  cache:
    key-prefix: putao:<service>                                    # ⚠️ 必须带服务名
```

### 3. 三大高频踩坑（**漏一个就启动失败**）

| # | 错误写法 | 现象 | 正确写法 |
|---|----------|------|----------|
| **A** | 只写 `spring.cloud.nacos.discovery.server-addr`，**漏写** `spring.cloud.nacos.config.server-addr` | config client 连 `127.0.0.1:9848` 反复超时，DataId 拉到的 content 是空 → DataSource/Redis 全部没法创建 | 两个 server-addr 都必须写，且 namespace/账号/密码也要**完整复制**一份到 config 节点 |
| **B** | 在 application.yml 里**硬编码 datasource.url** | 本地能起，但 dev 环境连 localhost 错乱；且密码进 git | url/username/password 全部放 Nacos，application.yml 只留 `driver-class-name` + `hikari` + `connection-init-sql` |
| **C** | application.yml 写 `spring.application.name: <service>` 而不是 `dating-<service>-service` | 服务注册到 Nacos 时名字跟其他服务不一致，网关/gRPC discovery 找不到 | 全服务统一 `dating-<service>-service` 命名 |

### 4. Nacos 上 DataId 命名约定

- 格式：`dating-<service>-service-dev.yaml`（**带 -dev 后缀**，跟 group=DEFAULT_GROUP 配套）
- Namespace：**`putao-dating-dev`**（葡萄的统一 namespace，dash 不是下划线）
- Group：**`DEFAULT_GROUP`**（不要自定义）
- 文件扩展名：`yaml`

### 5. Nacos DataID 必须包含的字段

```yaml
spring:
  datasource:
    url: jdbc:postgresql://38.76.188.242:5433/putao_dating_dev?currentSchema=<service>&sslmode=disable   # ⚠️ currentSchema 是服务名
    username: jianjian_test
    password: <Nacos 上，不进 git>
  data:
    redis:
      host: 38.76.188.242
      port: 6380
      password: <Nacos 上>
      database: 1
      timeout: 3000ms
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 2
  flyway:
    baseline-on-migrate: true
    baseline-version: 0
    placeholder-replacement: false
```

### 6. DB 命名约定（容易混）

- **库名（PG database）**：`putao_dating_dev`（**下划线**）
- **Schema**：`putao_dating_dev` 库下的 `<service>` 子 schema（`user` / `im` / `post` / `match` / `payment` / `gateway`），**不是 public**
- **Nacos namespace**：又是 dash：`putao-dating-dev`
- 三者关系：URL `jdbc:postgresql://...:5433/putao_dating_dev?currentSchema=<service>`，Nacos `tenant=putao-dating-dev`

### 7. 启动前自检 Checklist（启动失败时按顺序看）

1. `spring.cloud.nacos.config.server-addr` 写了没？（踩坑 A）
2. Nacos 上 `dating-<service>-service-dev.yaml` 这个 DataId 是否存在？内容是否非空？
3. PG 库 `putao_dating_dev` 里 `currentSchema` 指定的 schema 是否已建（Flyway 会自动建，但要先保证 URL 带 `currentSchema`）
4. Redis 端口 `6380`、密码、database=`1` 是否齐
5. Flyway 历史表是否停留在 failed 状态（需要 DROP schema 重建）

### 8. 复制现有服务时 application.yml 改哪 4 处

复制 `<existing-service>` → `<new-service>` 时只改这 4 个地方，其他全部保留：

1. `server.port` 默认值
2. `spring.application.name`
3. `spring.config.import` 里的 DataId
4. `app.cache.key-prefix`（`putao:<service>`）

**改完先按上面 7 项 Checklist 跑一遍再启动**。

---

## Vibe-Coding 文档组织规范

### 目录结构

```
doc/
├── README.md           # 文档总览（入口）
├── specs/              # 原始需求/设计文档
│   ├── post-service-design.md
│   └── user-service-design.md
├── plans/              # 实现计划（vibe-coding 前生成）
│   └── YYYY-MM-DD-<service>-<phase>.md
├── progress/           # 进度日志（session 后追加）
│   └── <service>-log.md
└── generated/          # AI 生成的辅助文档
    └── <service>-api.md
```

### Session 开始前

1. 如果是第一次做某个服务，先创建 `doc/plans/YYYY-MM-DD-<service>-p1.md`
2. 在 plan 中明确：本轮要实现哪些模块、优先级、预期产出

### Session 结束后

在 `doc/progress/<service>-log.md` 追加本轮内容：

```markdown
## 2026-07-14 (Session #1)

**目标**: 完成基础骨架 + proto 定义

**完成**:
- [x] 复制 example-service → post-service
- [x] 定义 9 个 RPC 接口

**遗留**:
- [ ] Nacos 配置还未填写密码

**AI 行为备注**:
- AI 把 getFriendUserIds 误实现成了同步调用，需要改成带超时和降级的版本
```

### 关键原则

1. **设计文档一次写好，后续只记录偏差**（不要反复改设计文档）
2. **进度日志追加式更新**，不要每次创建新文件
3. **主设计文档（specs/）是 source of truth**，generated/ 目录只是辅助
4. **每次 session 开始时，先读 progress/<service>-log.md 了解上下文**
